//konversi string dan Number

const value = `1`
const value2 = `2`
const sum = value + value2

document.writeln( `<p>${sum}</p>` )

//konversi dengan Function
//parselnt(string) = mengkonversi dari string ke number (bilangan bulat)
//parseFloat (string ) = konversi bilangan pecahan
//Number(string) = konversi dari string ke number
//number.toString() = konversi dari number ke string

//catatan = bisa disisipka di var atau di console